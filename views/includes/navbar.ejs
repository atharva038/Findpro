<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <div class="container-fluid">
    <a class="navbar-brand" href="/">
      <img
        src="/img/icon.png"
        alt=""
        width="90"
        height="90"
        alt="Team Member"
        class="rounded-circle mb-2"
      />
      <a href="/" class="nav-link me-2 ">KnockNFix</a>
    </a>
    <button
      class="navbar-toggler"
      type="button"
      data-bs-toggle="collapse"
      data-bs-target="#navbarNav"
      aria-controls="navbarNav"
      aria-expanded="false"
      aria-label="Toggle navigation"
    >
      <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
      <div class="navbar-nav">
        <a class="nav-link" href="/">Home</a>
        <a class="nav-link" href="/services">All Services</a>
        <!-- <a class="nav-link" href="/registerService">Register for new service</a> -->

        <% if (typeof currProvider !== "undefined" && currProvider) { %>
        <a class="nav-link" href="/dashboard">Dashboard</a>
        <% } %> <% if (typeof currCustomer !== "undefined" && currCustomer) { %>
        <a class="nav-link" href="/dashboard">Dashboard</a>
        <% } %>
      </div>
      <div class="navbar-nav ms-auto">
        <% if(!currUser){ %>
        <a class="nav-link" href="/login">Login</a>
        <a class="nav-link" href="/register">SignUp</a>
        <% } %> <% if(currUser) {%>
        <a class="nav-link" href="/logout">Logout</a>
        <% } %>
      </div>
    </div>
  </div>
</nav>
